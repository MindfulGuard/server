#NOTE THIS IS AN AUTO-GENERATED FILE, DO NOT EDIT IT.

import inspect

class BaseLocalization:
    def __init__(self):...
    

    @property
    def language_code(self) -> str:
        """        """
        raise NotImplementedError(f"{self.__class__.__name__}.{inspect.currentframe().f_code.co_name} method must be implemented in subclass")
    
    @property
    def successfully(self) -> str:
        """        """
        raise NotImplementedError(f"{self.__class__.__name__}.{inspect.currentframe().f_code.co_name} method must be implemented in subclass")
    
    @property
    def not_found(self) -> str:
        """        """
        raise NotImplementedError(f"{self.__class__.__name__}.{inspect.currentframe().f_code.co_name} method must be implemented in subclass")
    
    @property
    def data_not_valid(self) -> str:
        """        """
        raise NotImplementedError(f"{self.__class__.__name__}.{inspect.currentframe().f_code.co_name} method must be implemented in subclass")
    
    @property
    def registration_not_allowed(self) -> str:
        """        """
        raise NotImplementedError(f"{self.__class__.__name__}.{inspect.currentframe().f_code.co_name} method must be implemented in subclass")
    
    @property
    def registration_was_successful(self) -> str:
        """        """
        raise NotImplementedError(f"{self.__class__.__name__}.{inspect.currentframe().f_code.co_name} method must be implemented in subclass")
    
    @property
    def service_is_not_available(self) -> str:
        """        """
        raise NotImplementedError(f"{self.__class__.__name__}.{inspect.currentframe().f_code.co_name} method must be implemented in subclass")
    
    @property
    def server_error(self) -> str:
        """        """
        raise NotImplementedError(f"{self.__class__.__name__}.{inspect.currentframe().f_code.co_name} method must be implemented in subclass")
    
    @property
    def user_already_exists(self) -> str:
        """        """
        raise NotImplementedError(f"{self.__class__.__name__}.{inspect.currentframe().f_code.co_name} method must be implemented in subclass")
    
    @property
    def user_not_found(self) -> str:
        """        """
        raise NotImplementedError(f"{self.__class__.__name__}.{inspect.currentframe().f_code.co_name} method must be implemented in subclass")
    
    @property
    def user_found(self) -> str:
        """        """
        raise NotImplementedError(f"{self.__class__.__name__}.{inspect.currentframe().f_code.co_name} method must be implemented in subclass")
    
    @property
    def session_token_has_been_deleted(self) -> str:
        """        """
        raise NotImplementedError(f"{self.__class__.__name__}.{inspect.currentframe().f_code.co_name} method must be implemented in subclass")
    
    @property
    def failed_to_delete_token(self) -> str:
        """        """
        raise NotImplementedError(f"{self.__class__.__name__}.{inspect.currentframe().f_code.co_name} method must be implemented in subclass")
    
    @property
    def safe_was_successfully_created(self) -> str:
        """        """
        raise NotImplementedError(f"{self.__class__.__name__}.{inspect.currentframe().f_code.co_name} method must be implemented in subclass")
    
    @property
    def failed_to_create_a_safe(self) -> str:
        """        """
        raise NotImplementedError(f"{self.__class__.__name__}.{inspect.currentframe().f_code.co_name} method must be implemented in subclass")
    
    @property
    def unauthorized(self) -> str:
        """        """
        raise NotImplementedError(f"{self.__class__.__name__}.{inspect.currentframe().f_code.co_name} method must be implemented in subclass")
    
    @property
    def safe_was_successfully_updated(self) -> str:
        """        """
        raise NotImplementedError(f"{self.__class__.__name__}.{inspect.currentframe().f_code.co_name} method must be implemented in subclass")
    
    @property
    def failed_to_update_safe(self) -> str:
        """        """
        raise NotImplementedError(f"{self.__class__.__name__}.{inspect.currentframe().f_code.co_name} method must be implemented in subclass")
    
    @property
    def successful_login(self) -> str:
        """        """
        raise NotImplementedError(f"{self.__class__.__name__}.{inspect.currentframe().f_code.co_name} method must be implemented in subclass")
    
    @property
    def safe_has_been_successfully_deleted(self) -> str:
        """        """
        raise NotImplementedError(f"{self.__class__.__name__}.{inspect.currentframe().f_code.co_name} method must be implemented in subclass")
    
    @property
    def failed_to_delete_the_safe(self) -> str:
        """        """
        raise NotImplementedError(f"{self.__class__.__name__}.{inspect.currentframe().f_code.co_name} method must be implemented in subclass")
    
    @property
    def item_was_successfully_created(self) -> str:
        """        """
        raise NotImplementedError(f"{self.__class__.__name__}.{inspect.currentframe().f_code.co_name} method must be implemented in subclass")
    
    @property
    def failed_to_create_item(self) -> str:
        """        """
        raise NotImplementedError(f"{self.__class__.__name__}.{inspect.currentframe().f_code.co_name} method must be implemented in subclass")
    
    @property
    def failed_to_update_the_item(self) -> str:
        """        """
        raise NotImplementedError(f"{self.__class__.__name__}.{inspect.currentframe().f_code.co_name} method must be implemented in subclass")
    
    @property
    def item_has_been_successfully_updated(self) -> str:
        """        """
        raise NotImplementedError(f"{self.__class__.__name__}.{inspect.currentframe().f_code.co_name} method must be implemented in subclass")
    
    @property
    def item_was_successfully_deleted(self) -> str:
        """        """
        raise NotImplementedError(f"{self.__class__.__name__}.{inspect.currentframe().f_code.co_name} method must be implemented in subclass")
    
    @property
    def failed_to_delete_item(self) -> str:
        """        """
        raise NotImplementedError(f"{self.__class__.__name__}.{inspect.currentframe().f_code.co_name} method must be implemented in subclass")
    
    @property
    def item_was_successfully_added_to_favorites(self) -> str:
        """        """
        raise NotImplementedError(f"{self.__class__.__name__}.{inspect.currentframe().f_code.co_name} method must be implemented in subclass")
    
    @property
    def failed_to_update_favorite(self) -> str:
        """        """
        raise NotImplementedError(f"{self.__class__.__name__}.{inspect.currentframe().f_code.co_name} method must be implemented in subclass")
    
    @property
    def item_was_successfully_moved_to_safe(self) -> str:
        """        """
        raise NotImplementedError(f"{self.__class__.__name__}.{inspect.currentframe().f_code.co_name} method must be implemented in subclass")
    
    @property
    def failed_to_move_item_to_safe(self) -> str:
        """        """
        raise NotImplementedError(f"{self.__class__.__name__}.{inspect.currentframe().f_code.co_name} method must be implemented in subclass")
    
    @property
    def successfully_updated(self) -> str:
        """        """
        raise NotImplementedError(f"{self.__class__.__name__}.{inspect.currentframe().f_code.co_name} method must be implemented in subclass")
    
    @property
    def failed_to_update(self) -> str:
        """        """
        raise NotImplementedError(f"{self.__class__.__name__}.{inspect.currentframe().f_code.co_name} method must be implemented in subclass")
    
    @property
    def user_has_been_successfully_deleted(self) -> str:
        """        """
        raise NotImplementedError(f"{self.__class__.__name__}.{inspect.currentframe().f_code.co_name} method must be implemented in subclass")
    
    @property
    def failed_to_delete_user(self) -> str:
        """        """
        raise NotImplementedError(f"{self.__class__.__name__}.{inspect.currentframe().f_code.co_name} method must be implemented in subclass")
    
    @property
    def access_denied(self) -> str:
        """        """
        raise NotImplementedError(f"{self.__class__.__name__}.{inspect.currentframe().f_code.co_name} method must be implemented in subclass")
    
    @property
    def settings_have_been_successfully_updated(self) -> str:
        """        """
        raise NotImplementedError(f"{self.__class__.__name__}.{inspect.currentframe().f_code.co_name} method must be implemented in subclass")
    
    @property
    def failed_to_update_settings(self) -> str:
        """        """
        raise NotImplementedError(f"{self.__class__.__name__}.{inspect.currentframe().f_code.co_name} method must be implemented in subclass")
    
    @property
    def conflict(self) -> str:
        """        """
        raise NotImplementedError(f"{self.__class__.__name__}.{inspect.currentframe().f_code.co_name} method must be implemented in subclass")
    
class LocalizationEn(BaseLocalization):
    def __init__(self):...
    
    @property
    def language_code(self) -> str:
        return f"en"
    
    @property
    def successfully(self) -> str:
        return f"successfully"
    
    @property
    def not_found(self) -> str:
        return f"not found"
    
    @property
    def data_not_valid(self) -> str:
        return f"the data is not valid"
    
    @property
    def registration_not_allowed(self) -> str:
        return f"registration is not allowed"
    
    @property
    def registration_was_successful(self) -> str:
        return f"registration was successful"
    
    @property
    def service_is_not_available(self) -> str:
        return f"the service is not available"
    
    @property
    def server_error(self) -> str:
        return f"server error"
    
    @property
    def user_already_exists(self) -> str:
        return f"the user already exists"
    
    @property
    def user_not_found(self) -> str:
        return f"user not found"
    
    @property
    def user_found(self) -> str:
        return f"user found"
    
    @property
    def session_token_has_been_deleted(self) -> str:
        return f"the session token has been deleted"
    
    @property
    def failed_to_delete_token(self) -> str:
        return f"failed to delete token"
    
    @property
    def safe_was_successfully_created(self) -> str:
        return f"the safe was successfully created"
    
    @property
    def failed_to_create_a_safe(self) -> str:
        return f"failed to create a safe"
    
    @property
    def unauthorized(self) -> str:
        return f"unauthorized"
    
    @property
    def safe_was_successfully_updated(self) -> str:
        return f"the safe was successfully updated"
    
    @property
    def failed_to_update_safe(self) -> str:
        return f"failed to update the safe"
    
    @property
    def successful_login(self) -> str:
        return f"successful login"
    
    @property
    def safe_has_been_successfully_deleted(self) -> str:
        return f"the safe has been successfully deleted"
    
    @property
    def failed_to_delete_the_safe(self) -> str:
        return f"failed to delete the safe"
    
    @property
    def item_was_successfully_created(self) -> str:
        return f"the item was successfully created"
    
    @property
    def failed_to_create_item(self) -> str:
        return f"failed to create item"
    
    @property
    def failed_to_update_the_item(self) -> str:
        return f"failed to update the item"
    
    @property
    def item_has_been_successfully_updated(self) -> str:
        return f"the item has been successfully updated"
    
    @property
    def item_was_successfully_deleted(self) -> str:
        return f"the item was successfully deleted"
    
    @property
    def failed_to_delete_item(self) -> str:
        return f"failed to delete item"
    
    @property
    def item_was_successfully_added_to_favorites(self) -> str:
        return f"the item was successfully added to favorites"
    
    @property
    def failed_to_update_favorite(self) -> str:
        return f"couldn't add item to favorites"
    
    @property
    def item_was_successfully_moved_to_safe(self) -> str:
        return f"the item was successfully moved to the safe"
    
    @property
    def failed_to_move_item_to_safe(self) -> str:
        return f"failed to move item to safe"
    
    @property
    def successfully_updated(self) -> str:
        return f"successfully updated"
    
    @property
    def failed_to_update(self) -> str:
        return f"failed to update"
    
    @property
    def user_has_been_successfully_deleted(self) -> str:
        return f"the user has been successfully deleted"
    
    @property
    def failed_to_delete_user(self) -> str:
        return f"failed to delete user"
    
    @property
    def access_denied(self) -> str:
        return f"access denied"
    
    @property
    def settings_have_been_successfully_updated(self) -> str:
        return f"the settings have been successfully updated"
    
    @property
    def failed_to_update_settings(self) -> str:
        return f"failed to update settings"
    
    @property
    def conflict(self) -> str:
        return f"conflict"
    
class LocalizationRu(BaseLocalization):
    def __init__(self):...
    
    @property
    def language_code(self) -> str:
        return f"ru"
    
    @property
    def successfully(self) -> str:
        return f"успешно"
    
    @property
    def not_found(self) -> str:
        return f"не найдено"
    
    @property
    def data_not_valid(self) -> str:
        return f"неправильные данные"
    
    @property
    def registration_not_allowed(self) -> str:
        return f"регистрация запрещена"
    
    @property
    def registration_was_successful(self) -> str:
        return f"регистрация прошла успешно"
    
    @property
    def service_is_not_available(self) -> str:
        return f"сервис недоступен"
    
    @property
    def server_error(self) -> str:
        return f"ошибка сервера"
    
    @property
    def user_already_exists(self) -> str:
        return f"пользователь уже существует"
    
    @property
    def user_not_found(self) -> str:
        return f"пользователь не найден"
    
    @property
    def user_found(self) -> str:
        return f"пользователь найден"
    
    @property
    def session_token_has_been_deleted(self) -> str:
        return f"токен сеанса был удален"
    
    @property
    def failed_to_delete_token(self) -> str:
        return f"не удалось удалить токен"
    
    @property
    def safe_was_successfully_created(self) -> str:
        return f"сейф удачно создан"
    
    @property
    def failed_to_create_a_safe(self) -> str:
        return f"не удалось создать сейф"
    
    @property
    def unauthorized(self) -> str:
        return f"не авторизован"
    
    @property
    def safe_was_successfully_updated(self) -> str:
        return f"сейф успешно обновлен"
    
    @property
    def failed_to_update_safe(self) -> str:
        return f"не удалось обновить сейф"
    
    @property
    def successful_login(self) -> str:
        return f"удачный вход в систему"
    
    @property
    def safe_has_been_successfully_deleted(self) -> str:
        return f"сейф был успешно удален"
    
    @property
    def failed_to_delete_the_safe(self) -> str:
        return f"не удалось удалить сейф"
    
    @property
    def item_was_successfully_created(self) -> str:
        return f"элемент был успешно создан"
    
    @property
    def failed_to_create_item(self) -> str:
        return f"не удалось создать элемент"
    
    @property
    def failed_to_update_the_item(self) -> str:
        return f"не удалось обновить элемент"
    
    @property
    def item_has_been_successfully_updated(self) -> str:
        return f"элемент успешно обновлен"
    
    @property
    def item_was_successfully_deleted(self) -> str:
        return f"элемент был успешно удален"
    
    @property
    def failed_to_delete_item(self) -> str:
        return f"не удалось удалить элемент"
    
    @property
    def item_was_successfully_added_to_favorites(self) -> str:
        return f"элемент успешно добавлен в избранное"
    
    @property
    def failed_to_update_favorite(self) -> str:
        return f"не удалось добавить элемент в избранное"
    
    @property
    def item_was_successfully_moved_to_safe(self) -> str:
        return f"элемент был успешно перемещен в сейф"
    
    @property
    def failed_to_move_item_to_safe(self) -> str:
        return f"не удалось переместить элемент в сейф"
    
    @property
    def successfully_updated(self) -> str:
        return f"успешно обновлено"
    
    @property
    def failed_to_update(self) -> str:
        return f"не удалось обновить"
    
    @property
    def user_has_been_successfully_deleted(self) -> str:
        return f"пользователь успешно удален"
    
    @property
    def failed_to_delete_user(self) -> str:
        return f"не удалось удалить пользователя"
    
    @property
    def access_denied(self) -> str:
        return f"отказано в доступе"
    
    @property
    def settings_have_been_successfully_updated(self) -> str:
        return f"настройки были успешно обновлены"
    
    @property
    def failed_to_update_settings(self) -> str:
        return f"не удалось обновить настройки"
    
    @property
    def conflict(self) -> str:
        return f"конфликт"
    
class Localization:
    def __init__(self, locale: str = "en"):
        self.__locale = locale
    
    @property
    def default_locale(self) -> str:
        return "en"
    
    @property
    def current_locale(self) -> str:
        return self.__locale
    
    @property
    def locales(self) -> list[str]:
        return [
           "en",
           "ru",
        ]
    
    
    def of(self) -> BaseLocalization:
        
        if self.__locale == "en":
            return LocalizationEn()
                
        elif self.__locale == "ru":
            return LocalizationRu()
                
        else:
            raise ValueError(f"No {self.__locale} localization.")
        